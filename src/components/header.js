import React from "react"
import Header_Content from "./headerContent"
import { useSelector } from "react-redux"

export default function Header({ props }) {

  return <div className="header"><Header_Content />

  </div>
}
